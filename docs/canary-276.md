# Canary for issue 276

This file exists only to give a throwaway draft PR a diff. The PR is a
controlled experiment for the phantom draft-promotion investigation
(issue 276): after one `@codex review`, the shepherding session touches
nothing — no pushes, no replies, no `gh pr ready` — and watches whether
the PR is promoted out of draft anyway.

The PR must never be merged; it is closed when the observation window ends.
