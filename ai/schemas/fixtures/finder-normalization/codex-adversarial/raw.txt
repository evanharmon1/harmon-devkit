Reviewing branch changes against origin/main.

P0 scripts/round-push.sh:118 — the lock is released before the push is
confirmed, so a crash between the two leaves the branch pushed and the
round unrecorded. A resumed session then re-pushes the same round.

P2 docs/guides/codex-review.md — the backgrounding recipe still names the
retired `shepherd` stage.

There are no further P0 or P1 findings.
