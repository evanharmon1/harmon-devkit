# Docker Templates

Docker Compose stacks meant to be copied and adapted.

| Stack | Description |
| --- | --- |
| [`genericStack/`](./genericStack/) | Generic multi-service sandbox (Ubuntu, nginx, optional Postgres/memcached/Adminer stack) with `dc*` helper scripts |
| [`n8n-compose/`](./n8n-compose/) | n8n workflow automation behind Traefik with automatic HTTPS via Let's Encrypt |
